{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-jwt": "^6.0.0",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^3.6.0",
    "multer": "^1.4.2"
  },
  "scripts": {
    "start:auth": "node authServer.js",
    "start:server": "node server.js",
    "docker:build": "docker-compose build",
    "docker:start": "docker-compose up -d",
    "docker:stop": "docker-compose stop",
    "docker:remove": "docker-compose down && docker rmi backend_server backend_auth"
  }
}